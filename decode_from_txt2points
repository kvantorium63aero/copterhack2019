#перевод точек из txt в массив
def decoderOK():
    file = open('points.txt', 'r')
    mas = file.read().split('\n')
    points = []
    for i in range(len(mas) - 1):
        points.append([])
        
        
